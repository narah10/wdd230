//determine days between stored data and visit date ... today
//determine todays date
const millisecondsToDays = 8640000; // divide the difference between dates in milliseconds
// last visit ... stored in localstorage
// localStorage.getitem('lastvisit');
// determine number of days (last visit - date.now() / millisecondsToDays) .. .toFixed(0)
// display to wherever I want to
// need update tge stored date    localStorage.setItem('Lastvisit', Date.now());